# frozen_string_literal: true

source 'https://rubygems.org'

gemspec

# A Fiber scheduler is required at runtime but deliberately not a gem
# dependency. `async` comes in as a development dependency via the gemspec;
# swap in another implementation to develop against it:
# gem "itsi-scheduler", "~> 0.2", require: false

# Optional dependency for metrics - add this to enable metrics
# gem "async-utilization", ">= 0.3", "< 0.5"
